{% extends '@EasyAdmin/page/content.html.twig' %}

{% block page_title  %} Regroupement Article par Reference {% endblock  %} 

{% block page_content %}
 

<form action="{{ path('xlsx') }}" enctype="multipart/form-data" method="post">

    {{ form_row(form.brochure) }}
        <button type="submit" class="btn btn-primary">Submit</button>

{{ form_end(form) }}

    <table class="datagrid"> 
            <thead>
                <th> Reference</th>
                <th> Designation</th>
                <th> fournisseur</th>
                <th> Quantite</th> 
                <th> Action</th> 
            </thead>
        {% for produit in produits %}
            <tr>
                <td>
                    {{produit.reference}}
                </td>
                <td>
                    {{produit.designation}}
                </td>
                <td>
                    {{produit.nom}}
                </td>
                <td>
                    {{produit.qte}} unité
                </td> 
                <td>
                    <a href="{{ path('detail_reference_admin',{'reference':produit.reference}) }}" class="btn btn-primary"> Details </a> 
                </td>
                
                
            </tr>
        {% endfor %}
    </table>
  

{% endblock %}